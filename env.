from dotenv import load_dotenv
load_dotenv()

try:
    API_ID = int(os.environ.get('API_ID'))
    API_HASH = os.environ.get('API_HASH')
    STRING_SESSION = os.environ.get('STRING_SESSION', None)
    PING_URL = os.environ.get('PING_URL')

    # إضافة فحص للتأكدانو المتغيرات موجودة
    if not all([API_ID, API_HASH]):
        raise ValueError("تأكد من إعداد API_ID و API_HASH بشكل صحيح في متغيرات البيئة.")
except (ValueError, TypeError) as e:
    # لو الاعدادات غلط او في مشكلة حيتم عرض رسالة مفصلة عن الخطأ
    print(f"خطأ في إعدادات API: {e}")
    sys.exit(1)


#  إعدادات السورس 
SESSION_NAME = 'auto_poster_session'